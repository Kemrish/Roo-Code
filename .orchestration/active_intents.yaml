# .orchestration/active_intents.yaml

active_intents:
  - id: "INT-001"
    name: "JWT Authentication Migration"
    status: "IN_PROGRESS"
    priority: "HIGH"
    created_at: "2026-02-16T10:00:00Z"
    updated_at: "2026-02-16T14:30:00Z"
    
    owned_scope:
      - "src/auth/**"
      - "src/middleware/jwt.ts"
      - "!src/auth/legacy/**"  # Exclude legacy files
    
    constraints:
      - type: "TECHNICAL"
        rule: "Must not use external auth providers"
      - type: "COMPATIBILITY"
        rule: "Must maintain backward compatibility with Basic Auth"
      - type: "PERFORMANCE"
        rule: "Token validation must complete within 50ms"
      - type: "SECURITY"
        rule: "ALLOW_COMMAND:npm test"
    
    acceptance_criteria:
      - "Unit tests in tests/auth/ pass with >80% coverage"
      - "Integration tests for token refresh flow pass"
      - "No regression in existing Basic Auth endpoints"
    
    dependencies:
      - intent_id: "INT-002"  # User Management
        type: "BLOCKS"
      - intent_id: "INT-003"  # API Gateway
        type: "REQUIRES"
    
    metadata:
      owner: "auth-team"
      epic: "EPIC-2026-Q1"
      story_points: 13